package com.example.evcharging.data.local;

import androidx.room.Database;
import androidx.room.RoomDatabase;

@Database(entities = {}, version = 1)
public abstract class EVChargingDatabase extends RoomDatabase {
    
}
